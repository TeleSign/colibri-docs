import { ArgTypes, Canvas, Meta, Subtitle, Title } from '@storybook/blocks';
import * as ColBreadcrumbStories from './col-breadcrumb.stories';
import { Callout } from '../../_storybook/components';

<Meta of={ColBreadcrumbStories} />

<div className="col-doc__wrapper">
  <div className="col-doc__container">
    <Title>Breadcrumb</Title>
    The Colibri Breadcrumb system provides navigation components for displaying hierarchical navigation trails. It includes both container (`col-breadcrumb`) and item (`col-breadcrumb-item`) components for building complete navigation structures.

    <Subtitle>Table of contents</Subtitle>
    - [How to use it](#how-to-use-it)
    - [When to use it](#when-to-use-it)
    - [Component preview](#component-preview)
      - [Basic breadcrumb](#basic-breadcrumb)
      - [Custom separator](#custom-separator)
    - [Properties](#properties)
      - [col-breadcrumb properties](#col-breadcrumb-properties)
      - [col-breadcrumb-item properties](#col-breadcrumb-item-properties)
    - [Events](#events)
    - [Accessibility](#accessibility)
    - [Interact with the component](#interact-with-the-component)

    ## How to use it
    1. Install the packages:
        ```bash
        npm install @telesign/colibri
        ```
    2. Import the dependencies:
        ```typescript
        import { registerColibriComponents, registerAllComponents, ColBreadcrumb, ColBreadcrumbItem } from '@telesign/colibri';
        import '@telesign/colibri/styles/styles.css';
        ```
    3. Register the web components:
        ```typescript
        // Option 1: Register specific components
        registerColibriComponents([ColBreadcrumb, ColBreadcrumbItem]);

        // Option 2: Register all Colibri components
        registerAllComponents();
        ```
    4. Use it in your HTML:
        ```html
        <col-breadcrumb>
          <col-breadcrumb-item href="/">Home</col-breadcrumb-item>
          <col-breadcrumb-item href="/products">Products</col-breadcrumb-item>
          <col-breadcrumb-item current>Details</col-breadcrumb-item>
        </col-breadcrumb>
        ```

    ## When to use it
    Use the Breadcrumb system to:

    ### `col-breadcrumb` (Container)
    - **Provide navigation structure** - Creates the overall breadcrumb navigation container
    - **Manage separators** - Controls the separator character between all breadcrumb items
    - **Ensure accessibility** - Provides proper ARIA labels and navigation semantics
    - **Maintain consistency** - Ensures visual and behavioral consistency across all items

    ### `col-breadcrumb-item` (Items)
    - **Create navigation links** - Each item can link to a parent page in the hierarchy
    - **Show current location** - Mark the current page with the `current` attribute
    - **Handle SPA navigation** - Use router mode for client-side routing
    - **Provide contextual navigation** - Help users move up the site hierarchy

    ## Component preview

    ### Basic breadcrumb
    <div>
      The basic breadcrumb demonstrates both container and item functionality. The container manages the overall navigation structure while items provide individual navigation links.
    </div>
    <h4>Overview</h4>

    <Callout variant="tip" icon="💡">
      You can click on the "Show code" button in the bottom-right section of the following code snippets to see how to use the component.
    </Callout>

    <Canvas of={ColBreadcrumbStories.Default} />

    The breadcrumb system automatically:
    - Applies the separator to all child breadcrumb items
    - Provides proper navigation semantics
    - Handles responsive layout with wrapping when needed
    - Manages accessibility attributes

    ### Custom separator
    <div>
      You can customize the separator character displayed between breadcrumb items using the `separator` property. This allows you to match your design system's visual language.
    </div>

    <Canvas of={ColBreadcrumbStories.CustomSeparator} />

    Popular separator options include:
    - `/` (default) - Forward slash
    - `>` - Greater than symbol
    - `→` - Arrow
    - `•` - Bullet
    - `|` - Pipe
    - `»` - Double angle quote
    - `-` - Dash

    ### Text item
    <div>
      A basic breadcrumb item without any navigation functionality. This is typically used for items that shouldn't be clickable, though usually only the current page should be non-interactive.
    </div>

    <Canvas of={ColBreadcrumbStories.TextItem} />

    ### Link item
    <div>
      A breadcrumb item with an `href` attribute becomes a clickable link. This is the most common configuration for breadcrumb navigation.
    </div>

    <Canvas of={ColBreadcrumbStories.LinkItem} />

    The link item includes:
    - Hover effects for better interactivity feedback
    - Focus indicators for keyboard navigation
    - Standard link behavior with browser navigation

    ### Current page
    <div>
      The current page in a breadcrumb trail should be marked with the `current` attribute. This item is not clickable and has distinct styling to indicate it's the active page.
    </div>

    <Canvas of={ColBreadcrumbStories.CurrentPage} />

    Current page items:
    - Are not clickable (even if href is provided)
    - Have different text styling (bolder, different color)
    - Don't show hover effects
    - Include `aria-current="page"` for accessibility

    ### External link
    <div>
      Breadcrumb items can open in a new tab by setting the `newTab` property. This is useful for external links or when you want to preserve the user's current context.
    </div>

    <Canvas of={ColBreadcrumbStories.ExternalLink} />

    External links automatically include:
    - `target="_blank"` attribute
    - `rel="noopener noreferrer"` for security
    - Visual indicator (optional, through CSS)

    ### Router navigation
    <div>
      For single-page applications, use the `router` property to handle navigation without page refreshes. This prevents default link behavior and emits an event instead.
    </div>

    <Canvas of={ColBreadcrumbStories.RouterNavigation} />

    Router mode features:
    - Prevents default link navigation
    - Emits `breadcrumb-click` event with navigation details
    - Maintains link appearance and accessibility
    - Works with or without `href` attribute

    Example event handler:
    ```javascript
    document.addEventListener('breadcrumb-click', (event) => {
      const { value, label, index, href } = event.detail;
      // Handle navigation with your router
      myRouter.navigate(value || href);
    });
    ```

    ### Download link
    <div>
      While less common in breadcrumbs, items can be configured as download links using standard link attributes through the internal LinkHandler by setting the `downloadable` property to `true`, and the `filename` property to the desired filename.
    </div>
    <Callout variant="info" icon="ℹ️">
      This preview shows the download functionality of the current page in csv format. For the **full interactive experience** with working download of a file, visit the [Canvas tab](/story/atoms-breadcrumb--download-link).
    </Callout>

    <Canvas of={ColBreadcrumbStories.DownloadLink} />

    ## Properties

    ### `col-breadcrumb` properties
    <div>
      Properties for the breadcrumb container component that defines the overall navigation structure and appearance.
    </div>

    <ArgTypes of={ColBreadcrumbStories} include="^(aria-label|separator)$" />

    ### `col-breadcrumb-item` properties
    <div>
      Properties for individual breadcrumb items that control navigation behavior, styling, and interactivity.
    </div>

    <ArgTypes of={ColBreadcrumbStories} include="^(text|href|current|newtab|value|router|downloadable|filename)$" />

    ## Events

    ### breadcrumb-click

    The `breadcrumb-click` event is emitted when a breadcrumb item is clicked. This event bubbles and is composed, so it can be handled at any parent level.

    **Event detail structure:**
    ```typescript
    interface BreadcrumbClickDetail {
      value: string;     // The value prop of the clicked item
      label: string;     // The text content of the clicked item
      index: number;     // The index position within the breadcrumb
      href?: string;     // The href if provided
    }
    ```

    **Example usage:**
    ```javascript
    // Handle at container level
    breadcrumb.addEventListener('breadcrumb-click', (e) => {
      if (e.detail.value === 'home') {
        router.navigate('/');
      }
    });
    ```

    ## Accessibility

    The Breadcrumb system includes several built-in accessibility features:

    - **Semantic HTML** - Uses `<nav>` element to identify the breadcrumb as a navigation landmark
    - **ARIA label** - Customizable `aria-label` attribute for the navigation element (defaults to "Breadcrumb navigation")
    - **Screen reader support** - Includes hidden "You are here:" text for screen reader users
    - **Current page indication** - Uses `aria-current="page"` for the current item
    - **Keyboard navigation** - Ensures all interactive breadcrumb items are keyboard accessible

    Best practices:
    - Use descriptive aria-labels when multiple breadcrumbs exist on a page
    - Ensure breadcrumb items have sufficient color contrast
    - Keep the breadcrumb trail concise and logical
    - Always mark the current page item with the `current` attribute
    - Use descriptive, concise text for each breadcrumb item

    ## Interact with the component
    To interact with the component, test actions, verify accessibility compliance, and perform visual testing, navigate to the [Defaut](/story/atoms-breadcrumb--default) section.
  </div>
</div>
