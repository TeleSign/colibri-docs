import { ArgTypes, Canvas, Meta, Subtitle, Title } from '@storybook/blocks';
import LinkTo from '@storybook/addon-links/react';
import * as ColButtonStories from './col-button.stories';

<Meta of={ColButtonStories} />

<div className="col-doc__wrapper">
  <div className="col-doc__container">
    <Title>Button</Title>
    Buttons are clickable items used to trigger actions. They communicate calls to action to the user and allow users to interact with the pages in various ways.

    <Subtitle>How to use it</Subtitle>
    1. Install the packages:
        ```bash
        npm install @telesign/colibri
        ```
    2. Import the dependencies:
        ```typescript
        import { registerColibriComponents, ColButton } from '@telesign/colibri';
        import '@telesign/colibri/styles/styles.css';
        ```
    3. Register the web component:
        ```typescript
        registerColibriComponents([ColButton]);
        ```
    4. Use it in your HTML:
        ```html
        <col-button>
          Text
        </col-button>
        ```

    <Subtitle>When to use it</Subtitle>
    The "button" component is a fundamental element of the user interface that allows users to perform actions within an application by simply clicking or tapping. Here is a summary of its main features:

    * **Action triggering**: Buttons allow users to initiate specific actions, such as submitting forms, navigating to another screen, confirming a selection or making a purchase.
    * **Visibility and prominence**: Buttons are visible and prominent in the user interface so that users can easily identify the available actions.
    * **Visual feedback**: When a button is clicked or tapped, users receive visual feedback, such as a color change, animation or tapping effect, to indicate that the action has been successfully initiated.
    * **Clear labeling**: Buttons are often labeled with descriptive text or icons indicating the action to be performed when clicked, which helps users understand their purpose.
    * **Varied styles and sizes**: Buttons can have different styles and sizes to suit the design and visual hierarchy of the application.

    <Subtitle>Component preview</Subtitle>
    You can click on the **Show code** button in the bottom-right section of the following code snippets to see how to use the component:

    <h4>Variant: Default</h4>
    <Canvas of={ColButtonStories.Default}/>

    <h4>Color: Primary Button</h4>
    <Canvas of={ColButtonStories.PrimaryButton} />

    <h4>Color: Large Danger Outlined Button</h4>
    <Canvas of={ColButtonStories.LargeDangerOutlineButton} />

    <h4>Color: Small Success Plain Button</h4>
    <Canvas of={ColButtonStories.SmallSuccessPlainButton} />

    <h4>Color: Icon Button</h4>
    <Canvas of={ColButtonStories.IconButton} />

    <h4>Color: Dropdown Button</h4>
    <Canvas of={ColButtonStories.ButtonWithDisclosure} />

    <h4>Color: Button with Badge</h4>
    <Canvas of={ColButtonStories.ButtonWithBadge} />

    <h4>Color: Button with Everything</h4>
    <Canvas of={ColButtonStories.ButtonWithEverything} />

    <Subtitle>Properties</Subtitle>

    <ArgTypes of={ColButtonStories} />

    <Subtitle>Interact with the component</Subtitle>
    To interact with the component, test actions, verify accessibility compliance, and perform visual testing, navigate to the <LinkTo title={ColButtonStories.default.title} story="default">Default</LinkTo> section.

  </div>
</div>
