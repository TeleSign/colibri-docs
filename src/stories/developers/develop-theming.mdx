import { Meta } from '@storybook/blocks';
import { Header } from '../../blocks/Header.jsx';

<Meta title="developers/Theming/Introduction " />

<Header isDoc title="Introduction" />

## Advanced Theming System

### Component-Specific Theming

Components can receive theme overrides through the `tokens` property:

```typescript
const customTokens = {
  colors: {
    primary: '#FF0000',
    secondary: '#00FF00'
  }
};

html`
  <col-button .tokens=${customTokens}>
    Custom Themed Button
  </col-button>
`;
```

### Theme Inheritance

Components automatically inherit theme context from their parents:

```typescript
html`
  <div data-theme="dark">
    <!-- Will use dark theme -->
    <col-button>Dark Button</col-button>

    <!-- Override to light theme -->
    <col-button theme="light">Light Button</col-button>
  </div>
`;
```

### Custom Token Generation

The token generation system supports:

1. Global tokens
2. Component-specific tokens
3. Theme variations
4. Dynamic token references

Example token structure:
```javascript
const tokens = {
  colors: {
    primary: '#007AFF',
    secondary: '#5856D6'
  },
  components: {
    buttons: {
      backgroundColor: 'var(--col-colors-primary)',
      textColor: '#FFFFFF'
    }
  }
};
```